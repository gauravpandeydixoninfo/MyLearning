import React from "react";

//loading file should be of name loading.js only, it can't be changed
//press ctrl and load sign in chrome to see loading file due to high speed.

export default function Page(){
    console.log("loader runed");
    return(
        <h1> Loading Details... </h1>  
    )
}