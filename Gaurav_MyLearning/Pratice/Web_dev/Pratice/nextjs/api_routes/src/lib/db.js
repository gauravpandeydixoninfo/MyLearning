const {usernamee, password}= process.env;
// const mongoose= require("mongoose");
// export const connectionStr=`mongodb+srv://${usernamee}:${password}@cluster0.svf1ctl.mongodb.net/product?retryWrites=true&w=majority`

export const connectionStr='mongodb://localhost/inotebook';
// mongoose.connect(connectionStr);
